<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type Button}">
        <Setter Property="Content" Value="{Binding Command.Text, RelativeSource={RelativeSource Self}}" />
        <Setter Property="Width" Value="Auto" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" CornerRadius="1" BorderBrush="#FF8B8B8B" BorderThickness="1" Padding="{TemplateBinding Padding}" Cursor="Hand">
                        <Border.Background>
                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                <GradientStop Color="#FFE0E0E0" Offset="1"/>
                                <GradientStop Color="#FFF0F0F0"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <ContentPresenter x:Name="grid" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" TargetName="border" Value="#eee" />
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" TargetName="border" Value="0.8"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" TargetName="grid" Value="0.25"/>
                            <Setter Property="Background" TargetName="border" Value="#eee" />
                        </Trigger>

                        <Trigger Property="IsDefault" Value="True">
                            <Setter Property="Foreground" Value="#FFF" />
                            <Setter Property="BorderBrush" TargetName="border" Value="#FF2A766A" />
                            <Setter Property="Background" TargetName="border">
                                <Setter.Value>
                                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                        <GradientStop Color="#FF2A766A" Offset="1"/>  <!-- dark -->
                                        <GradientStop Color="#FF4AA99A"/> <!-- light -->
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock HorizontalAlignment="Left" Text="{Binding}" Padding="8,3,8,3"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>